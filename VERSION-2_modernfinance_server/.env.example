# Server Configuration
NODE_ENV=production
PORT=8080
API_VERSION=v1

# CORS Configuration 
# For testing/development: Set to * to allow all origins
# For production: Set specific origins like https://your-app.com,https://another-domain.com
CORS_ORIGINS=*

# Logging Configuration
LOG_LEVEL=info
LOG_FORMAT=json

# Redis Configuration
# Railway provides REDIS_URL automatically when you add Redis
# For local development, use the individual settings below
REDIS_URL=
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_TLS=false

# Security (will add proper auth later)
JWT_SECRET=temporary-dev-secret-change-in-production
ALLOWED_ORIGINS=*

# Market Data APIs (Optional - will fallback to Yahoo/mock if not provided)
# Get your free API key from: https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=

# Yahoo Finance doesn't require an API key

# LLM APIs (Optional - will use intelligent fallbacks if not provided)
# At least one is recommended for best AI features
# OpenAI: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Anthropic: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Google: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=60

# Cache Configuration
CACHE_TTL_SHORT=300
CACHE_TTL_DEFAULT=3600
CACHE_TTL_LONG=86400

# Feature Flags
ENABLE_REAL_DATA=true
ENABLE_AI_AGENTS=true
ENABLE_CACHING=true
ENABLE_COLLABORATION=true

# Railway Deployment Notes:
# 1. Railway automatically provides REDIS_URL when you add Redis service
# 2. Use Railway's environment variables UI to set API keys securely
# 3. The server will work without API keys using fallback data
# 4. For production, change CORS_ORIGINS to your specific domains